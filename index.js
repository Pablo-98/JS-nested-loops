// creating a working solution with nested loops (Time complexity: O(n^))


function twoSum(nums,target) {
    // declaring a variable named 'result' and setting it equal to an empty array
    let result = [];

    //the first for loop takes i, sets it to zero, and if i is less than 
    //the length of the number a user inputs 
    for(let i = 0; i < nums.length; i++) {
        for(let j = i + 1; j<nums.length; j++ ){
            if (nums[i] + nums[j] === target) {

            }
        }
    }
}